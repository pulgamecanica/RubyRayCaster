<%= form_with(model: room_player) do |form| %>
  <% if room_player.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room_player.errors.count, "error") %> prohibited this room_player from being saved:</h2>

      <ul>
        <% room_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :player_id, style: "display: block" %>
    <%= form.text_field :player_id %>
  </div>

  <div>
    <%= form.label :game_room_id, style: "display: block" %>
    <%= form.text_field :game_room_id %>
  </div>

  <div>
    <%= form.label :morale, style: "display: block" %>
    <%= form.number_field :morale %>
  </div>

  <div>
    <%= form.label :energy, style: "display: block" %>
    <%= form.number_field :energy %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
